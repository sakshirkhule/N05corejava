package org.tnsif.statickeyword;
/*static variable if any variable is non static
 * and we have to access the variable inside the main function then we can make tht
 * variable as static*/
public class StaticVariable {
	static int num=45;

	public static void main(String[] args) {
	System.out.print(num);

	}

}
